// 1. rustc --version: current latest version of RUST : 1.62.1
// 2. The fn keyword is used to define a function. 
// 3. The main() is a predefined function that acts as an entry point to the program. 
/* 4. println! is a predefined macro in Rust. It is used to print a string (here Hello) to the console. 
 Macro calls are always marked with an exclamation mark – !. */

 fn main(){
    println!("Rust says Hello to TutorialsPoint !!");
  
    println!(); // prints just a newline
    println!("hello "); //prints hello
    println!("format {} arguments", "some"); //prints format some arguments
 }